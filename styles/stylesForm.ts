import { StyleSheet } from "react-native";


export const stylesForm = StyleSheet.create({
    form: {
        width: '100%',
        flexDirection: 'row',
        marginTop: 16,
        marginBottom: 16,
        gap: 8
      }
})